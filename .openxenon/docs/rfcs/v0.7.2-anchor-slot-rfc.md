# v0.7.2 sub-RFC — Anchor / Slot 文档绑定机制

> **RFC 状态**：📝 Draft（待 review）
> **目标版本**：v0.7.2（第三个 v0.7 minor）
> **前置依赖**：v0.7.0 + v0.7.1
> **核心交付**：Markdown 文档锚点（Anchor）+ Domain Slot 双向绑定 + Insight evidence 反查
> **来源**：[ADR-0029](../../../../forges/sprints/archive/docs-tmp-era/decisions/0029-anchor-slot-doc-binding.md) · docs_tmp/ssot-domain-1.md (2026-06-17)

---

## 0. 背景与动机

当前 Insight `evidenceChain[]` 仅指向 `trace.jsonl:142`（行号）。工程师看到 Insight 时，需要：

1. 跳到 trace.jsonl 看上下文
2. 手动到 docs/ 找业务文档
3. 手动匹配 Domain term

v0.7.2 引入 **Anchor（文档锚点）+ Slot（域插槽）** 双向绑定，让 Insight 能直接反查到 docs 章节：

```
Insight "密码不能明文存储"
  → evidence → Domain term "register"
            → Slot @anchor/api-register
                → docs/zh-cn/api.md#api-register
```

---

## 1. 双向锚点契约

### 1.1 文档侧（Anchor）

Markdown H2 / H3 可声明 `{#anchor-id}` slug：

```markdown
<!-- docs/zh-cn/api.md -->
## 用户注册 API {#api-register}

`POST /register` 必须满足：
- 邮箱格式正确
- 密码长度 ≥ 8
```

### 1.2 域侧（Slot）

Domain OXL term 引用 anchor：

```oxl
domain "MemberContext" {
  term "register" {
    anchor "@doc/api-register"          // 物理引用 docs/ 锚点
    invariant "邮箱格式正确"              // 自然语言
    invariant "密码长度 ≥ 8"
  }
}
```

### 1.3 双向校验（编译期）

- `oxn domain validate` 检查 `@doc/<id>` 是否存在
- `oxn docs validate` 检查文档中 `{#<id>}` 是否被 Domain 引用
- 任一缺失 → `E_DOC_ANCHOR_ORPHAN` / `E_DOMAIN_SLOT_DANGLING`

---

## 2. 实现细节

### 2.1 文档解析器（v0.7.2 新增）

`packages/engine/src/oxl/md-bridge/anchor-extractor.ts`：

```ts
export function extractAnchors(mdContent: string): Anchor[] {
  // 扫描 ## ... {#id} 或 ### ... {#id}
  // 返回 { level, text, anchor, line }[]
}
```

### 2.2 Domain 编译器扩展

`packages/engine/src/oxl/md-bridge/compilers/domain-compiler.ts`：

```ts
// 解析 term.anchor 字段
if (term.anchor) {
  const ref = parseAnchorRef(term.anchor)
  // ref.kind === 'doc' → 检查 docs/<ref.path>#<ref.id> 存在
  // ref.kind === 'term' → 跨 term 引用
}
```

### 2.3 Insight 反查链路

`packages/engine/src/Insight/graph-builder.ts` 扩展：

```ts
function buildInsightGraph(insightId, data) {
  // ...
  for (const slot of data.slots ?? []) {
    lines.push(`  T -.->|anchor| ANCHOR[${slot.anchor}]`)
    lines.push(`  ANCHOR -.->|docs| DOC[${slot.docPath}#${slot.anchorId}]`)
  }
  // ...
}
```

---

## 3. 物理布局（v0.7.2 新增/修改）

### 3.1 新增

```
packages/engine/src/oxl/md-bridge/
├── anchor-extractor.ts             # 文档锚点提取
└── __tests__/
    └── anchor-extractor.test.ts    # 6 cases

packages/engine/src/Insight/
├── slot-resolver.ts                # Anchor ↔ Slot 双向解析
└── __tests__/
    └── slot-resolver.test.ts       # 8 cases

packages/cli/src/commands/
├── docs-validate.ts                # oxn docs validate（检查 orphan anchor）
└── __tests__/
    └── docs-validate-e2e.test.ts   # 4 cases
```

### 3.2 修改

- `packages/engine/src/oxl/md-bridge/compilers/domain-compiler.ts` — 加 anchor 字段
- `packages/engine/src/Insight/graph-builder.ts` — Anchor 反查节点
- `packages/cli/src/commands/domain.ts` — `validate` 子命令加 anchor 完整性检查
- `packages/cli/src/errors/iap-error.ts` — 新增 `E_DOC_ANCHOR_ORPHAN` / `E_DOMAIN_SLOT_DANGLING`

### 3.3 Hall v0.5 Asset 影响图扩展

`docs/.vitepress/theme/components/AssetsPanel.vue`：

- Asset 详情页加 "Anchored Docs" 卡片
- 显示此 Asset 关联的所有 docs 章节（双向链接）

---

## 4. 测试统计

| 类型 | 文件 | 数量 |
|---|---|---|
| 单元 | anchor-extractor | 6 |
| 单元 | slot-resolver | 8 |
| 单元 | domain-compiler anchor 字段 | 4 |
| E2E | docs-validate | 4 |
| 集成 | Hall AssetsPanel | 2 |
| **合计** | — | **24** |

**v0.7.2 验收门槛**：≥ 2,068 pass（v0.7.1 末 2,044 + 24）

---

## 5. 风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| Anchor ID 冲突 | 中 | 中 | slug 唯一性校验（编译期） |
| 文档重命名导致 anchor 失效 | 中 | 中 | 重命名时自动检测 + 警告 |
| Hall 渲染双链断裂 | 低 | 低 | 链接存在性校验（health check） |

---

## 6. v0.7.2 不做

- 自动生成 anchor ID（用户手工标注）
- Anchor 反向查找图谱（v0.8.0 Asset 知识库范围）
- 跨项目 anchor 引用（v0.8 Skill 远程 Registry）

---

## 7. 参考

- [ADR-0029](../../../../forges/sprints/archive/docs-tmp-era/decisions/0029-anchor-slot-doc-binding.md)
- [v0.7 Emergence RFC](./v0.7-emergence-rfc.md) §2.6 Asset 影响图
- [v0.7+ Roadmap Overview](../v0.7-plus-roadmap/overview.md) §2 v0.7.2 阶段

---

**作者**：docs-tmp-cleanup work @ 2026-07-05
**目标发布**：v0.7.2 = 2027-01-15