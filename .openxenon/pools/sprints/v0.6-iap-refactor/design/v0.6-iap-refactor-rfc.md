# v0.6 IAP 架构重构 RFC

> **日期**：2026-06-28
> **状态**：🟡 RFC 提案（v1.2，待 user 拍板后进入实施）
> **基础**：v0.5 RFC · IAP 三轴架构 · v0.4 资产统一 MD
> **作者**：opencode（与 user 协作，2026-06-28）
> **范围**：v0.6.0 架构重构，~8 周，10 sub-PR 串行

---

# What — OXN 哲学模型

## 1. 四结构实体（E1-E4）

OXN 由 4 个**结构实体（Entity）**构建而成。这是**哲学理念层**，不直接映射代码目录或 L 层级：

```
┌──────────────────────────────────────────────────────────────────┐
│  E4 · Insight — 涌现层（1+1>2）                                    │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━                                       │
│  跨 Work 综合推理 → 资产级建议（新增/改进/废弃）                     │
│  哲学底色：钱学森系统论 · 整体论                                     │
│  不可还原 · 只能综合推理 · v0.6 哲学定位 + CLI 占位，v0.7+ 推理    │
└────────────────────────┬──────────────────────────────────────────┘
                         │ 综合集成
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  E3 · Engine — 引擎基座                                            │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━                                       │
│  整个 OpenXenon = Engine                                           │
│  独立验证主权（frozen.json + content_hash + planLock）              │
│  工程师体现实现在 L0-L2，L3 是工具入口                               │
└────────────────────────┬──────────────────────────────────────────┘
                         │ submit → 执行 IAP 循环
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  E2 · Work — 动态协作空间                                          │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━                                       │
│  Intent（工程师声明意图 + 选 Asset 边界）                           │
│  Align（AI 多轮对齐，Round × N → 每轮拆 M 个 Tasks）               │
│  Proof（Engine 独立验证 → verdict）                                │
│  Round：verdict fail → 回到 Intent 调整 → 新一轮 Align             │
│  工程师 ↔ AI 共建                                                  │
└────────────────────────┬──────────────────────────────────────────┘
                         │ ref 引用
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│  E1 · Asset — 静态边界                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━                                       │
│  Domain / Blueprint / Stack                                       │
│  硬约束（planLock + content_hash），不被 Work 改写                   │
│  工程师维护。形似 OpenSpec specs/，语义对立——被引用而非被改写        │
└──────────────────────────────────────────────────────────────────┘
```

**4 句定位**：

> **Asset 是边界**（工程师维护的硬约束，不被 Work 改写）。
> **Work 是协作**（IAP 三阶段多轮循环，工程师与 AI 共建）。
> **Engine 是公证**（独立验证主权，IAP 范式内核 + 适配层）。
> **Insight 是涌现**（1+1>2，钱学森系统论整体论，AI 跨 Work 综合推理）。

## 2. 工程实现模型（L0-L3）

E1-E4 结构在代码中通过 L0-L3 实现。L0-L3 是**工程分层的文档概念**，不直接对应物理目录——实际目录用各自名称（`kernel/`、`oxl/`、`infra/`、`Asset/`、`Intent/`、`Align/`、`Proof/`、`Insight/`、`cli/` 等）：

```
┌────────────────────────────────────────────────────────────────────────┐
│  L3: Tools & Applications (工具与应用层)                                │
│      - 面向用户/Agent 的直接交互界面                                     │
│      - 包含: CLI (oxn 入口), Skills (扩展能力), Daemon (后台强监督)       │
│      - E2 Work + E4 Insight 的入口编排在此层完成                          │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │ 驱动 / 调用
┌───────────────────────────────────▼────────────────────────────────────┐
│  L2: Engine Core Logic (引擎核心业务层)                                  │
│      - E1-E4 全部实现在此层（5 大业务模块 + IAP 状态机）                  │
│      - Asset / Intent / Align / Proof / Insight 各为独立业务模块         │
│      - 按 DDD 模块化组织：Service/<Domain>/{index,create,list,...}.ts   │
│      - 驱动 IAP 范式状态流转与可信证明的核心中枢                           │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │ 调度 / 依赖
┌───────────────────────────────────▼────────────────────────────────────┐
│  L1: Language DSL & Infra (操作基座与语言层)                             │
│      - OXL: OpenXenon Language 的 DSL 编译器/解析器                     │
│      - Infra: 文件/AST 扫描探针，为引擎提供 OS/硬件级操作基座            │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │ 依赖 / 自举
┌───────────────────────────────────▼────────────────────────────────────┐
│  L0: Logic Kernel (逻辑内核层)                                           │
│      - 纯逻辑、无副作用的底层状态机与微型内核                              │
│      - 包含: 状态冻结机制（Freeze）、frozen.json 原子读写、Schema/Contract│
└────────────────────────────────────────────────────────────────────────┘
```

**关键边界**：
- **E1-E4 是哲学结构**（理念层），不直接映射代码目录
- **L0-L3 是工程分层**（概念层），描述代码的依赖方向，不直接对应物理目录
- **物理目录**直接用各自名称：`kernel/`、`oxl/`、`infra/`、`src/service/<Domain>/`、`cli/`、`daemon/`
- E1-E4 的**全部实现**都在 Engine（L0-L2），L3 是工具入口

## 3. Engine 内部职责映射

| E 实体 | L2 业务模块 | 核心能力 |
|---|---|---|
| E1 Asset | `service/Asset/` | Domain/Blueprint/Stack CRUD + sync + compile（硬约束边界维护）|
| E2 Work（Intent 阶段） | `service/Intent/` | 创建 Work + 选 Asset 边界 + 多轮调整 |
| E2 Work（Align 阶段） | `service/Align/` | AI 多轮对齐（Round）+ Tasks 执行 + 状态机 |
| E2 Work（Proof 阶段）| `service/Proof/` | 3 阶段探针执行协议 + frozen.json + verdict.md |
| E4 Insight | `service/Insight/` | 跨 Work 综合推理 + 资产级建议（涌现层）|
| 辅助 | `service/Pool/` | 5 类池 + review/approve/reject（资产改进闸门）|

## 4. Work = IAP 范式的最小完整单元

**v0.6 最重要的哲学收敛**：

```
任何工作 = 一次 work = IAP 周期的实例
  ├─ 创建 Domain？     →  Asset 模式 work（Intent=指定资产类型，Align=落盘，Proof=校验）
  ├─ 开发新功能？       →  Develop 模式 work（Intent=选边界，Align=Round 对齐写代码，Proof=跑探针）
  ├─ 跑一次验收？       →  Proof 模式 work（Intent=声明探针，Align=准备，Proof=跑探针写 frozen.json）
  └─ 做一次总结？       →  Insight 模式 work（Intent=定义维度，Align=扫描，Proof=产出报告）
```

**Skill 极简**：原来 3 个 Skill（oxn-cli / oxn-work / oxn-proof）→ **唯一 1 个 Skill `oxn-work`**

## 5. Work 内部 IAP 三阶段 + Round 多轮循环

Work 内部按 IAP 范式分 3 个 Work 模式（v0.6 从 4 个收敛为 3 个——Insight 升为 E4 涌现层）：

| 模式 | Intent 阶段 | Align 阶段 | Proof 阶段 | 典型用例 |
|---|---|---|---|---|
| **Asset 模式** | 创建 work，指定资产类型（domain/blueprint/stack） | AI 填写内容 + 落盘 | OXN 校验语法 + 写 planLock | 建 MemberContext Domain |
| **Develop 模式** | 选资产边界 + 定 goal + constraints | AI 写代码（Explore/Develop/Fix/Onboarding 4 子模式） | 跑 lint/test/build + verdict | 开发新功能 |
| **Proof 模式** | 声明探针集合 + 准备环境 | AI 执行准备 | OXN 跑探针 + 写 frozen.json | check-deploy 验收 |

**Round 机制**（v0.6 最小实现）：

```
Work (my-feature)
├── Round 1: intent + align + proof → verdict FAIL (type-check)
│   └── oxn work next-round → 回到 Intent 调整
├── Round 2: intent(adjust) + align + proof → verdict FAIL (lint)
│   └── oxn work next-round
├── Round 3: intent(refine) + align + proof → verdict PASS
└── finalize
```

**v0.6 最小实现**：
- Work 状态记录 `currentRound` 字段
- 新增 CLI 命令 `oxn work next-round <work>`——显式开启新一轮 IAP
- Verify fail 后**不自动循环**（避免无限循环），手动触发
- 每轮保留独立 `round-n/` 子目录（记录 intent/align/proof 快照）
- 全自动循环留 v0.7+

## 6. CLI 双路径设计

保留独立子命令作为快速通道，新增 `oxn work` 作为严谨路径：

```
oxn
├── init / config / install-skill           ← 工具型
│
├── domain / blueprint / stack             ← 资产层快速通道（保留，绕过 work）
├── proof                                   ← OXN 证明快速通道（保留）
├── insight                                 ← OXN 涌现通道（保留）
│
├── work                                    ← IAP 主入口（3 模式分发）
│   ├── create <w> --type asset|develop|proof
│   ├── add-task / validate / lock / unlock
│   ├── run / submit / status / finalize / context
│   ├── next-round <w>                     ← v0.6 新增：开启新一轮 IAP
│   └── ...
│
└── token / probe / pool / dev              ← 保留
```

## 7. 物理目录：Config 软迁移

**默认布局**（v0.6 新项目）：
```
.openxenon/
├── config.json                   ← assetRoot + assetDirs 配置
├── assets/                       ← 默认 assetRoot
│   ├── domain/<Name>.oxn
│   ├── blueprint/<name>.oxn
│   └── stack/<name>.oxn
├── works/<w>/
│   ├── work.oxn
│   ├── round-1/                  ← v0.6 Round 快照（新增）
│   │   ├── intent.md
│   │   └── align/log.jsonl
│   ├── round-2/
│   └── .run/
│       ├── state.json
│       └── frozen.json
├── proofs/<p>/
├── pools/
└── ...
```

**兼容旧项目**（v0.5 `.openxenon/{domains,blueprints}/`）：
```bash
oxn config set assetRoot ''
oxn config set assetDirs.domain domains
oxn config set assetDirs.blueprint blueprints
```

---

# Why — 旧 IAP 三轴的局限

## 1. 现状矛盾

### 1.1 旧 IAP 三轴叙事混淆"资产"和"流程"

旧叙事：
```
Intent 轴 ──→ Domain + Blueprint (由工程师创建，作为轴的产物)
Align  轴 ──→ Work + Task + Part
Proof  轴 ──→ Proof + Verdict
```

**问题**：Domain + Blueprint 实质是工程师沉淀的**可复用硬约束边界**，不应绑定在某个 IAP 轴上。把"资产"当作"Intent 轴的产物"掩盖了它"稳定约束"的本质。

### 1.2 Skill 数量过多

旧 3 个 Skill（oxn-cli / oxn-work / oxn-proof）让工程师被迫判断"这个工作属于哪个轴"再选 Skill。但实际上所有工作都可以建模为 work（IAP 周期）。Skill 的"分轴"是过度拆分。

### 1.3 物理目录命名与哲学脱节

旧 `.openxenon/{domains,blueprints,works,proofs}/` 按 IAP 轴分类，让"资产"看起来像"流程的副产品"。

### 1.4 旧 Insight 语义狭窄

v0.5 RFC 把 Insight 设计为"跨 proof 涌现模式"——读 frozen.json + probe-stats.json → emergent patterns。但实际更宏观的价值是"跨 Work 综合推理 + 资产级改进建议"。**Insight 应该是涌现层（E4），不是 Work 的子模式。**

## 2. 设计原则

**原则 1：E1-E4 哲学先行**。4 结构实体作为 OXN 的顶层概念，不直接映射代码目录。

**原则 2：L0-L3 单向依赖**。Tools (L3) → Engine (L2) → OXL+Infra (L1) → Kernel (L0)。

**原则 3：Work = IAP 最小单元**。所有工作都是 Work，Skill 只需 `oxn-work`。Round 是护城河（区别于 OpenSpec 的单次线性流程）。

**原则 4：Insight = 涌现层**。哲学上归属 E4（整体论），实现上归属 L2 Engine。v0.6 哲学定位 + CLI 占位，涌现推理留 v0.7+。

**原则 5：Asset = 硬约束边界**。区别于 OpenSpec 的"被改写的规范"——OXN Asset 有 planLock + content_hash 三层锁，被 Work 引用但不被改写。

## 3. 核心反转

| 维度 | 旧（v0.5） | 新（v0.6） |
|---|---|---|
| 核心隐喻 | IAP 三轴 | 4 结构实体（E1-E4）+ L0-L3 工程分层 |
| E1 Asset 定位 | Intent 轴的产物 | 工程师维护的硬约束边界 |
| E2 Work 定位 | Align 轴的执行空间 | IAP 周期承载体（含 Round 多轮循环） |
| E3 Engine 定位 | 分散的 Runtime 组件 | 独立验证主权基座 |
| E4 Insight 定位 | 跨 proof 涌现模式 | **涌现层**（钱学森系统论 1+1>2） |
| Skill 数量 | 3 个 | **1 个（oxn-work）** |
| 物理目录分类 | 按 IAP 轴 | 按"资产 vs 流程"分层（Config 软迁移） |
| Work 模式 | 4 个（含 Insight Mode） | **3 个**（Asset/Develop/Proof）+ E4 Insight 独立 |
| Round | 无 | **v0.6 最小实现**（currentRound + oxn work next-round） |

---

# How — 实施 PR 拆分

## PR-0: RFC v0.5 → v0.6 升级

1. 本 RFC 文件（E1-E4 + L0-L3 双层叙事）
2. v0.5 RFC 保留作历史参考
3. `.changes/0-6-0-iap-refactor.md` changelog 占位

## PR-1: Asset Config + 路径解析

实现 `oxn config assetRoot` + `assetDirs` schema + fallback 兼容旧项目。

详见 [v0.6 RFC §PR-1](#)（略，同 v1.0 版）。

## PR-2: CLI 适配 + 快速通道 + Round 状态机

CLI 子命令适配新 config 路径解析 + `oxn work next-round` 命令。

**Round 最小实现**：
- Work 状态结构加 `currentRound: number` 字段
- `state.json` 加 `roundHistory: RoundRecord[]`（每轮 verdict + failures + 时间戳）
- CLI 命令 `oxn work next-round <w>`：记录当前轮、自增 round、打开 Intent 编辑
- **不做全自动循环**（手动触发）
- `oxn work finalize` 时汇总所有 round 供 Insight 消费

**受影响文件**：
- `service/Intent/`：refine-boundaries.ts（调整边界入口）
- `service/Align/`：run.ts + submit.ts（Round 感知）
- `src/kernel/schemas/state-schema.ts`：加 round 字段
- `src/cli/work.ts`：加 `next-round` 子命令

## PR-3: 文档 SSOT 重构（3 文档 + 同步现有）

- 新增 `work.md`（IAP 核心 + 3 模式 + 8 阶段流程 + Round）
- 新增 `asset.md`（E1 硬约束边界 + OpenSpec 对比）
- 新增 `insight.md`（E4 涌现层 + 钱学森系统论哲学）
- 重写 `core-concepts.md`（4 结构实体 + L0-L3 工程分层）
- 重写 `architecture.md`（E1-E4 + L0-L3 双层叙事 + Config 软迁移）
- 重写 `glossary.md`（E1-E4 术语 + L 术语更新）
- 删除 `intent.md` / `align.md` / `proof.md`
- 更新 vitepress sidebar

## PR-4: Skill 极简（oxn-work only）

- 删除 `oxn-cli` / `oxn-proof` 两个 Skill
- `oxn-work` 完全重写（3 模式 + Round + IAP 范式 + E1-E4 哲学必读章节）
- Skill 教学里加"哲学必读：OXN 的终极目标不是 spec 与实现一致，而是工程智慧的整体涌现"

## PR-5a: Service 拆分 — Asset Module

- 新建 `src/service/Asset/`（DDD 模块化：index.ts + create.ts/list.ts/validate.ts/...）
- 改造 `src/cli/domain.ts` (1237→200) + `src/cli/blueprint.ts` (1080→200) + 新建 `src/cli/stack.ts` (200)

## PR-5b: Service 拆分 — Intent + Align Module

- 新建 `src/service/Intent/` + `src/service/Align/`
- 改造 `src/cli/work.ts` (3801→400)，新建 `src/cli/intent.ts` (250) + `src/cli/align.ts` (150)

## PR-5c: Service 拆分 — Proof Module

- 新建 `src/service/Proof/`
- 改造 `src/cli/proof.ts` (1112→250)

## PR-5d: Service 拆分 — Insight + Pool Module

- 新建 `src/service/Insight/` + `src/service/Pool/`
- 改造 `src/cli/insight.ts` (513→150) + `src/cli/pool*.ts` 5 文件合并 → `src/cli/pool.ts` (200)

## PR-6: 集成测试 + 发版 v0.6.0

- 全仓库 e2e 测试
- `bun test` / `bun run typecheck` / `bun run lint` 全绿
- `bun scripts/verify-skill-structure.sh` (适配 1 Skill)
- `bun run docs:dev` 验证
- 发版 v0.6.0 + changelog

---

# 实施路线图

| PR | 名称 | 范围 | 依赖 | 周次 |
|---|---|---|---|---|
| **PR-0** | RFC | E1-E4 + L0-L3 双层叙事 + changelog | — | W1 |
| **PR-1** | Asset Config | config schema + 路径解析 fallback | — | W1 |
| **PR-2** | CLI + Round | CLI path 适配 + Round 最小状态机 | 依赖 PR-1 | W2-W3 |
| **PR-3** | Docs Rebuild | 3 文档 + core/architecture/glossary 重写 | 依赖 PR-0 | W3 |
| **PR-4** | Skill Converge | oxn-work only + 哲学必读 | 依赖 PR-3 | W4 |
| **PR-5a** | Service Asset | `service/Asset/` 模块 | 依赖 PR-2 | W4-W5 |
| **PR-5b** | Service Work | `service/Intent/` + `service/Align/` | 依赖 PR-5a | W5-W6 |
| **PR-5c** | Service Proof | `service/Proof/` | 依赖 PR-5b | W6 |
| **PR-5d** | Service Insight/Pool | `service/Insight/` + `service/Pool/` | 依赖 PR-5c | W7 |
| **PR-6** | Release v0.6.0 | e2e + 发版 | 依赖全部 | W8 |

**分支策略**：
- 主分支：`feat/v0.6-iap-refactor`（从 v0.5 拉出）
- 子分支命名：`feat/v0.6-t<N>-<slug>`
- 串行合并：PR-0 → PR-1 → PR-2 → PR-3 → PR-4 → PR-5a → PR-5b → PR-5c → PR-5d → PR-6
- PR-0 与 PR-1 可并行

**总工作量**：~8 周（含 Round 状态机 + Insight 哲学落盘）

---

# 决策记录

| 决策 | 结论 | 理由 |
|---|---|---|
| 哲学模型表达？ | **E1-E4 结构实体**（不用 Layer 避免与 Lx 混淆） | "Layer" 在哲学/工程双层叙事里容易歧义 |
| 工程分层表达？ | **L0-L3 概念层**（不直接对应物理目录） | 物理目录用各自名称（kernel/、oxl/、infra/、Asset/ 等） |
| E1-E4 实现在哪？ | **全部在 Engine（L0-L2）**，L3 是工具入口 | 哲学结构与代码实现分层——理念 vs 工程 |
| OpenXenon 的整体定位？ | **轻量级人机协作工具**（L3 Tools 层对外），**核心 Engine**（L0-L2 对内） | 用户门槛降低；"工具"对外，"引擎"对内 |
| Engine 内部实现组织？ | **DDD 模块化**：`service/<Domain>/{index,create,list,...}.ts` | 用户决策：现代 JS/TS 模块化，非 class-based |
| Round 多轮 IAP 循环？ | **v0.6 最小实现**（currentRound + oxn work next-round） | 护城河能力，v0.6 不做全自动循环 |
| Insight 涌现推理？ | **v0.6 哲学定位 + CLI 占位**，涌现推理 v0.7+ | 底色先立，实现可推后 |
| IAP 术语？ | **对外对内均保留 Intent/Align/Proof** | 用户锁定：不替换为 Spec-Build-Verify |
| 物理目录合并？ | **config 软迁移**（默认 assets/，兼容 fallback） | 避免破坏性 mv |
| OpenSpec 对比？ | **在 asset.md / work.md 显式声明**（3 个护城河） | 防止被归类为"OpenSpec 变体" |
| v0.5 RFC 处理？ | **保留作历史，新 v0.6 RFC 完整重写** | v0.6 RFC 是全新设计 |

---

# 范围之外

1. **Insight 涌现推理**（AI 跨 Work 综合推理）— v0.7+ 实现
2. **Round 全自动循环**— v0.6 手动触发，v0.7+ 自动
3. **Web UI / gRPC API**— v0.7+
4. **Asset 版本管理**— 仅 planLock + content_hash
5. **CLI 子命令破坏性删除**— 快速通道全部保留
6. **物理目录强制迁移**— config + fallback 软迁移

---

# 参考

- [v0.6 Service 层设计稿](./v0.6-service-layer-design.md)（独立设计文档）
- [v0.5 RFC](../v0.5-proof-insight-loop/design/v0.5-proof-insight-loop-rfc.md)（历史）
- [v0.4 RFC](../v0.4-unify-md/design/v0.4-unify-md-rfc.md)（历史）
- [docs/zh-cn/core-concepts.md](../../../docs/zh-cn/core-concepts.md)
- [docs/zh-cn/architecture.md](../../../docs/zh-cn/architecture.md)
