---
type: domain
id: <name>
version: 0.1.0
status: draft

# 🆕 Asset Paper 4 字段（v0.6.1-alpha.1 引入）
abstract: |
  TODO: 一句话描述本限界上下文的业务边界。
  TODO: 关键业务场景（2-3 行）
references: []          # 依赖的 Asset ID 列表（DAG 校验）
citations: 0            # Engine 自动维护（被引用次数）
auditTrail:
  - version: 0.1.0
    date: <YYYY-MM-DD>
    author: <engineer>
    changes: 初始模板
---

# Domain Body (论文正文)
# TODO: 添加 Term / Ban / Invariant 章节

domain "<name>" {
  description = "TODO: 一句话描述本限界上下文的业务边界"

  # Term 章节（最小 1 个，推荐 3+）
  term {
    "TODO_Term1": "TODO: domain term definition（核心实体名 + 1 句解释）"
    # 示例（取消注释即用）：
    # "Member": "注册会员实体，含基本信息 + 认证状态"
    # "Account": "会员的登录凭证，绑 Member.id"
    # "Order": "订单实体，含商品列表 + 支付状态"
  }

  # Ban 章节（最小 0 个，推荐 2+）
  ban {
    # "TODO_BannedTerm1"
    # "TODO_BannedTerm2"
    # 示例：
    # "User"          # 禁止使用 User（统一用 Member）
    # "Customer"      # 禁止使用 Customer
    # "AccountHolder" # 禁止使用 AccountHolder
  }

  # Invariant 章节（最小 1 个，推荐 3+）
  invariant {
    "TODO: business invariant rule 1"
    # 示例：
    # "任何 member 状态变更必须记录到 audit_log"
    # "同一邮箱在同一 context 内不可重复注册"
    # "支付接口必须具备幂等性（idempotency_key）"
  }
}
