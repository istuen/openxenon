---
type: stack
id: <name>
version: 0.1.0
status: draft

# 🆕 Asset Paper 4 字段（v0.6.1-alpha.1 引入）
abstract: |
  TODO: 一句话描述技术栈（语言 + runtime + 工具链）。
  TODO: 关键约束（2-3 行）
references: []          # 依赖的 Asset ID 列表
citations: 0            # Engine 自动维护
auditTrail:
  - version: 0.1.0
    date: <YYYY-MM-DD>
    author: <engineer>
    changes: 初始模板
---

# Stack Body (论文正文)
# TODO: 描述 runtime / linter / test 三类约束

stack "<name>" {
  description = "TODO: 一句话描述本技术栈"

  # Runtime 章节（最小 1 个）
  runtime "typescript" {
    version = ">=5.0.0"
    # 示例（取消注释即用）：TypeScript 5.0+
  }
  # runtime "node" {
  #   version = ">=20.0.0"
  # }

  # Linter 章节（最小 0 个，推荐 1+）
  linter "biome" {
    config = "biome.json"
    # 示例（取消注释即用）：biome
  }
  # linter "eslint" {
  #   config = "eslint.config.js"
  # }

  # Test 章节（最小 0 个，推荐 1+）
  test "bun-test" {
    command = "bun test"
    coverage = "@oxn/probes/test-pass"
    # 示例（取消注释即用）：bun test
  }
  # test "vitest" {
  #   command = "vitest run"
  #   coverage = "@oxn/probes/coverage-threshold"
  # }
}
