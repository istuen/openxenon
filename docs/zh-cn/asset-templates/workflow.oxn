---
type: workflow
id: <name>
version: 0.1.0
status: draft

# 🆕 Asset Paper 4 字段（v0.6.1-alpha.1 引入）
abstract: |
  TODO: 一句话描述本技术流程的步骤与依赖。
  TODO: 适用场景（2-3 行）
references: []          # 依赖的 Asset ID 列表
citations: 0            # Engine 自动维护
auditTrail:
  - version: 0.1.0
    date: <YYYY-MM-DD>
    author: <engineer>
    changes: 初始模板（原 blueprint 重命名为 workflow）
---

# Workflow Body (论文正文)
# TODO: 描述每个 slot 的职责 + 依赖关系

workflow "<name>" {
  description = "TODO: 一句话描述本技术流程"

  # Slot 章节（最小 1 个，推荐 3+）
  # 依赖关系：DAG 无环校验

  # 示例（取消注释即用）：标准 dev-workflow
  slot "build"   { deps = [] }              # 编译/构建
  slot "test"    { deps = ["build"] }      # 单元测试
  slot "lint"    { deps = ["build"] }      # 代码检查
  slot "verify"  { deps = ["test", "lint"] } # 集成验证（需 test + lint 都通过）
}
