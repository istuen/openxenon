---
type: roadmap
id: <name>
version: 0.1.0
status: draft

# 🆕 Asset Paper 4 字段（v0.6.1-alpha.1 引入）
abstract: |
  TODO: 一句话描述本 Roadmap 的导航目的（哪些 Asset 优先读取、按什么顺序）。
  TODO: 维护策略（手工 vs 自动更新，v0.6.1-alpha.1 阶段是手工）。
# 🆕 注意：references 留空（不参与 DAG 校验）
# 但 body 内含 Asset 路径/链接（人工维护或后续实现解析）
references: []
citations: 0            # 被多少 Asset 反向引用（拓扑指标）
auditTrail:
  - version: 0.1.0
    date: <YYYY-MM-DD>
    author: <engineer>
    changes: 初始模板
---

# Roadmap Body (论文正文)
# 注意：Roadmap 的"连接"通过 body 内的 Asset 路径表达
# 不通过 frontmatter.references（避免循环 DAG 校验）

# Roadmap: <Name>

## 核心常读（必入 Stable Prefix）
# 这些 Asset 每次 Work 都应注入到 AI 上下文
- TODO: `<path-to-asset>.oxn` — TODO: 一句话描述
- TODO: `<path-to-asset>.oxn` — TODO: 一句话描述

# 示例（取消注释即用）：
# - `domain/core.oxn` — 项目核心领域边界
# - `stack/nodejs.oxn` — 运行时环境约束

## 按需加载（任务相关时再注入）
# 这些 Asset 根据 Work 类型选择性注入
- TODO: `<path-to-asset>.oxn` — TODO: 一句话描述
- TODO: `<path-to-asset>.oxn` — TODO: 一句话描述

# 示例（取消注释即用）：
# - `workflow/dev-workflow.oxn` — 标准开发流程
# - `library/axios-docs.oxn` — Axios 文档聚合（v0.6.3+ 引入）

## 近期变更提示
# 标注近期有版本升级或引用关系变更的 Asset
# 供 AI Agent 关注影响面
- TODO: Asset 名 vX.Y → vX.Y（变更描述），影响 XXX
- TODO: Asset 名 vX.Y → vX.Y（变更描述），影响 XXX

# 示例（取消注释即用）：
# - `domain/payment-core` 从 v1.0 → v1.1（新增幂等性约束），影响所有付款相关 Workflow
# - `library/axios-docs` 从 v1.0 → v1.1（升级 axios 1.7.0 API），影响所有 HTTP 相关 Work
