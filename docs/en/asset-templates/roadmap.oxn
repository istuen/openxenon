---
type: roadmap
id: <name>
version: 0.1.0
status: draft

# 🆕 Asset Paper 4 fields (v0.6.1-alpha.1)
abstract: |
  TODO: One-line description of this Roadmap's navigation purpose
  (which Assets are read first, in what order).
  TODO: Maintenance strategy (manual vs auto; v0.6.1-alpha.1 is manual).
# 🆕 Note: references is empty (does not participate in DAG validation)
# But body contains Asset paths/links (manually maintained or future parser)
references: []
citations: 0            # Citation count (how many Assets reverse-reference)
auditTrail:
  - version: 0.1.0
    date: <YYYY-MM-DD>
    author: <engineer>
    changes: Initial template
---

# Roadmap Body (paper main text)
# Note: Roadmap's "connections" are expressed via Asset paths in body
# NOT via frontmatter.references (avoids cyclic DAG validation)

# Roadmap: <Name>

## Core reads (must be in Stable Prefix)
# These Assets should be injected into AI context for every Work
- TODO: `<path-to-asset>.oxn` — TODO: One-line description
- TODO: `<path-to-asset>.oxn` — TODO: One-line description

# Examples (uncomment to use):
# - `domain/core.oxn` — Project core domain boundary
# - `stack/nodejs.oxn` — Runtime environment constraints

## On-demand loads (injected when Work-relevant)
# These Assets are selectively injected based on Work type
- TODO: `<path-to-asset>.oxn` — TODO: One-line description
- TODO: `<path-to-asset>.oxn` — TODO: One-line description

# Examples (uncomment to use):
# - `workflow/dev-workflow.oxn` — Standard dev workflow
# - `library/axios-docs.oxn` — Axios docs aggregation (v0.6.3+)

## Recent change alerts
# Mark Assets with recent version upgrades or reference changes
# Help AI Agent be aware of impact scope
- TODO: Asset name vX.Y → vX.Y (change description), impacts XXX
- TODO: Asset name vX.Y → vX.Y (change description), impacts XXX

# Examples (uncomment to use):
# - `domain/payment-core` v1.0 → v1.1 (added idempotency constraint), impacts all payment-related Workflow
# - `library/axios-docs` v1.0 → v1.1 (upgraded axios 1.7.0 API), impacts all HTTP-related Work
