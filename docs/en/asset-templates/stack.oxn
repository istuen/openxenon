---
type: stack
id: <name>
version: 0.1.0
status: draft

# 🆕 Asset Paper 4 fields (v0.6.1-alpha.1)
abstract: |
  TODO: One-line description of tech stack (language + runtime + tooling).
  TODO: Key constraints (2-3 lines)
references: []          # Dependent Asset ID list
citations: 0            # Engine auto-maintained
auditTrail:
  - version: 0.1.0
    date: <YYYY-MM-DD>
    author: <engineer>
    changes: Initial template
---

# Stack Body (paper main text)
# TODO: Describe runtime / linter / test three categories of constraints

stack "<name>" {
  description = "TODO: One-line description of this tech stack"

  # Runtime section (min 1)
  runtime "typescript" {
    version = ">=5.0.0"
    # Example (uncomment to use): TypeScript 5.0+
  }
  # runtime "node" {
  #   version = ">=20.0.0"
  # }

  # Linter section (min 0, recommend 1+)
  linter "biome" {
    config = "biome.json"
    # Example (uncomment to use): biome
  }
  # linter "eslint" {
  #   config = "eslint.config.js"
  # }

  # Test section (min 0, recommend 1+)
  test "bun-test" {
    command = "bun test"
    coverage = "@oxn/probes/test-pass"
    # Example (uncomment to use): bun test
  }
  # test "vitest" {
  #   command = "vitest run"
  #   coverage = "@oxn/probes/coverage-threshold"
  # }
}
