---
type: workflow
id: <name>
version: 0.1.0
status: draft

# 🆕 Asset Paper 4 fields (v0.6.1-alpha.1)
abstract: |
  TODO: One-line description of this technical workflow's steps and dependencies.
  TODO: Applicable scenarios (2-3 lines)
references: []          # Dependent Asset ID list
citations: 0            # Engine auto-maintained
auditTrail:
  - version: 0.1.0
    date: <YYYY-MM-DD>
    author: <engineer>
    changes: Initial template (renamed from blueprint to workflow)
---

# Workflow Body (paper main text)
# TODO: Describe each slot's responsibility + dependencies

workflow "<name>" {
  description = "TODO: One-line description of this technical workflow"

  # Slot section (min 1, recommend 3+)
  # Dependency relations: DAG acyclic validation

  # Example (uncomment to use): standard dev-workflow
  slot "build"   { deps = [] }              # compile / build
  slot "test"    { deps = ["build"] }      # unit test
  slot "lint"    { deps = ["build"] }      # code check
  slot "verify"  { deps = ["test", "lint"] } # integration verify (need both test + lint)
}
