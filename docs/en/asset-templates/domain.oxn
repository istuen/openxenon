---
type: domain
id: <name>
version: 0.1.0
status: draft

# 🆕 Asset Paper 4 fields (v0.6.1-alpha.1)
abstract: |
  TODO: One-line description of this bounded context's business boundary.
  TODO: Key business scenarios (2-3 lines)
references: []          # Dependent Asset ID list (DAG validation)
citations: 0            # Engine auto-maintained (citation count)
auditTrail:
  - version: 0.1.0
    date: <YYYY-MM-DD>
    author: <engineer>
    changes: Initial template
---

# Domain Body (paper main text)
# TODO: Add Term / Ban / Invariant sections

domain "<name>" {
  description = "TODO: One-line description of this bounded context"

  # Term section (min 1, recommend 3+)
  term {
    "TODO_Term1": "TODO: domain term definition (core entity name + 1 sentence)"
    # Examples (uncomment to use):
    # "Member": "Registered member entity, with profile + auth status"
    # "Account": "Member's login credentials, bound to Member.id"
    # "Order": "Order entity, with product list + payment status"
  }

  # Ban section (min 0, recommend 2+)
  ban {
    # "TODO_BannedTerm1"
    # "TODO_BannedTerm2"
    # Examples:
    # "User"          # Prohibit User (use Member instead)
    # "Customer"      # Prohibit Customer
    # "AccountHolder" # Prohibit AccountHolder
  }

  # Invariant section (min 1, recommend 3+)
  invariant {
    "TODO: business invariant rule 1"
    # Examples:
    # "Any member state change must be logged to audit_log"
    # "Same email cannot register twice in the same context"
    # "Payment API must be idempotent (idempotency_key)"
  }
}
