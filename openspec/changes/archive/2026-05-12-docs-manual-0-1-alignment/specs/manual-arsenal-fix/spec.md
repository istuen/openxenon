# manual-arsenal-fix

## MODIFIED Requirements

### Requirement: 替换"Core Zod Schema 校验"描述

**FROM:**

```markdown
校验后的资产由 Core 进行 Zod Schema 校验，确保格式符合规范。
```

**TO:**

```markdown
校验后的资产由 Kernel 进行 Schema 校验，确保格式符合规范。
```
