# manual-cli-ref-fix

## MODIFIED Requirements

### Requirement: 替换"Core 引擎生命周期"描述

**FROM:**

```markdown
## oxn daemon

管理全局 Core 引擎的生命周期。
```

**TO:**

```markdown
## oxn daemon

管理全局 Daemon 进程的生命周期（0.2 目标，0.1 阶段可选）。
```
