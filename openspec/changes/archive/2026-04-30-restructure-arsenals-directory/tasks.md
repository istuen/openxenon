## 1. 规范定义

- [x] 1.1 在 `src/core/arsenals-paths.ts` 中定义新的目录结构常量 `ArsenalDirectoryStructure`
- [x] 1.2 定义资产路径解析函数，支持新旧路径检测

## 2. 读取兼容

- [x] 2.1 修改资产读取函数，优先查找新路径，回退到旧路径
- [x] 2.2 添加路径检测逻辑，判断资产存在于哪个路径

## 3. 写入更新

- [x] 3.1 修改资产写入函数，统一写入新路径结构
- [x] 3.2 创建资产时自动生成 `<asset-name>/draft.yaml` 目录结构

## 4. 迁移已有资产

- [x] 4.1 创建 `oxn arsenal migrate` 命令，用于一次性迁移
- [x] 4.2 实现迁移逻辑：遍历 `arsenals/<type>/draft/` 和 `arsenals/<type>/canonical/` 下的所有 `.yaml` 文件
- [x] 4.3 对每个旧资产文件，在新路径 `arsenals/<type>/<asset-name>/` 下创建对应版本文件
- [x] 4.4 迁移完成后删除旧文件（可选，支持 `--keep-old` 保留旧文件）
- [x] 4.5 添加迁移日志，记录每个资产的迁移结果
- [x] 4.6 验证迁移后所有资产可正常读取

## 5. 测试验证

- [x] 5.1 编写单元测试，覆盖新旧路径读取场景
- [x] 5.2 验证新资产创建使用新结构