## No New Requirements

This change is a file rename only with no functional changes to requirements.

**Impact:**
- `src/api/standards-draft.ts` → `src/api/arsenal-draft.ts`
- Import path update in `src/skills/oxn-forge.ts`

No new capabilities or modified requirements are introduced.