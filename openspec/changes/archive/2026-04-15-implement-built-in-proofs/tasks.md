## 1. 基础架构搭建

- [x] 1.1 定义 Proof 接口和类型系统（包括 ProofResult、ProofInput、ProofOutput 等核心类型）
- [x] 1.2 创建内置探针内存字典的数据结构和注册机制
- [x] 1.3 定义自定义探针的标准参数格式（JSON schema）

## 2. L1 文件系统层探针实现

- [x] 2.1 实现 `fs_exists` 探针（文件存在性探测，使用 stat 系统调用）
- [x] 2.2 实现 `fs_not_exists` 探针（文件不存在性探测）
- [x] 2.3 实现 `fs_content_match` 探针（内容正则匹配探测，读取文件并执行正则匹配）
- [x] 2.4 实现 `fs_parseable` 探针（语法可解析探测，调用语言原生语法检查器）

## 3. L2 进程执行层探针实现

- [x] 3.1 实现 `exec_exit_zero` 探针（进程零退出码探测，执行命令并检查退出码）
- [x] 3.2 实现 `exec_stdout_match` 探针（标准输出匹配探测，捕获 stdout 并执行正则匹配）

## 4. L3 运行时状态层探针实现

- [x] 4.1 实现 `db_query_bool` 探针（数据库标量查询探测，直接走底层驱动执行 SQL）
- [x] 4.2 实现 `env_exists` 探针（环境变量探测，读取 process.env 或 .env 文件）

## 5. L4 网络拓扑层探针实现

- [x] 5.1 实现 `http_status` 探针（HTTP 状态码探测，发起 TCP/HTTP 握手并检查状态码）

## 6. 自定义探针执行机制

- [x] 6.1 实现项目级自定义探针目录扫描和注册（`.xenonix/proofs/`）
- [x] 6.2 实现全局级自定义探针目录扫描和注册（`~/.xenonix/custom-proofs/`）
- [x] 6.3 实现自定义探针隔离执行机制（使用 `Bun.spawn` 创建子进程）
- [x] 6.4 实现 stdin 参数传递机制（将参数以 JSON 格式传递给子进程）
- [x] 6.5 实现进程退出码收割逻辑（仅读取退出码，不解析 stdout/stderr）
- [x] 6.6 实现自定义探针超时控制机制（防止探针无限期执行）

## 7. 探针调度逻辑实现

- [x] 7.1 实现三段式降级查找逻辑（内置 → 项目级 → 全局级 → 熔断）
- [x] 7.2 实现内置探针内存字典查找（O(1) 时间复杂度）
- [x] 7.3 实现项目级自定义探针文件查找（在 `.xenonix/proofs/` 目录中查找 `.ts` 文件）
- [x] 7.4 实现全局级自定义探针文件查找（在 `~/.xenonix/custom-proofs/` 目录中查找 `.ts` 文件）
- [x] 7.5 实现终极熔断机制（返回 `ProofNotFoundError` 并判定 Step 为 FAILED）
- [x] 7.6 实现项目级探针优先逻辑（项目级同名探针覆盖全局级）

## 8. 伪格探针防护机制

- [x] 8.1 在探针注册时拒绝 `llm_judge` 类型的探针
- [x] 8.2 在探针注册时拒绝 `complex_business_logic_check` 类型的探针
- [x] 8.3 在探针注册时拒绝 `code_style_score` 类型的探针
- [x] 8.4 实现探针名称黑名单机制（维护禁止的探针类型列表）

## 9. CLI 指令实现

- [x] 9.1 实现 `xn proof-list` CLI 指令的命令解析和路由
- [x] 9.2 实现内置探针列表输出（标记为 `[built-in]`）
- [x] 9.3 实现项目级自定义探针列表输出（标记为 `[project]`）
- [x] 9.4 实现全局级自定义探针列表输出（标记为 `[global]`）
- [x] 9.5 实现探针列表排序逻辑（内置 → 项目级 → 全局级）
- [x] 9.6 实现空目录处理逻辑（项目级或全局级目录不存在时不报错）

## 10. 日志记录机制

- [x] 10.1 实现内置探针调用日志记录（探针名称、类型、执行结果）
- [x] 10.2 实现自定义探针调用日志记录（探针名称、类型、文件路径、执行结果）
- [x] 10.3 实现熔断事件日志记录（探针名称、查找路径、熔断原因）

## 11. 错误处理和异常管理

- [x] 11.1 定义 `ProofNotFoundError` 错误类型
- [x] 11.2 实现自定义探针执行失败的错误处理（不影响 Core 主进程）
- [x] 11.3 实现探针参数验证错误处理
- [x] 11.4 实现探针超时错误处理

## 12. 编译和打包配置

- [x] 12.1 配置 Bun 打包脚本，将内置探针熔铸进单一二进制文件
- [x] 12.2 验证内置探针在编译后不可在文件系统中寻址
- [x] 12.3 验证内置探针在编译后仍然可以正确执行

## 13. 单元测试和集成测试

- [x] 13.1 为 L1 文件系统层探针编写单元测试
- [x] 13.2 为 L2 进程执行层探针编写单元测试
- [x] 13.3 为 L3 运行时状态层探针编写单元测试
- [x] 13.4 为 L4 网络拓扑层探针编写单元测试
- [x] 13.5 为三段式降级查找逻辑编写集成测试
- [x] 13.6 为自定义探针隔离执行机制编写集成测试
- [x] 13.7 为 `xn proof-list` CLI 指令编写测试

## 14. 文档编写

- [x] 14.1 编写内置探针使用文档（每个探针的功能、参数、示例）
- [x] 14.2 编写自定义探针开发指南（如何编写、存储、测试自定义探针）
- [x] 14.3 编写 Proof 架构设计文档（安全模型、物理边界、执行机制）
- [x] 14.4 编写 `xn proof-list` CLI 使用文档
