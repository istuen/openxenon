{
  "name": "openxenon",
  "version": "1.0.0",
  "description": "OpenXenon - 面向大语言模型的工程化控制引擎",
  "author": "OpenXenon Team",
  "license": "MIT",
  "type": "module",
  "main": "./dist/cli.js",
  "types": "./dist/types/index.d.ts",
  "bin": {
    "oxn": "./dist/oxn"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "build": "bun build ./src/cli/index.ts --compile --outfile dist/oxn && cp -r src/arsenals dist/arsenals",
    "build:all": "bun run build:linux && bun run build:macos && bun run build:windows",
    "build:linux": "bun build ./src/cli/index.ts --compile --target=bun-linux-x64 --outfile dist/oxn-linux",
    "build:macos": "bun build ./src/cli/index.ts --compile --target=bun-darwin-x64 --outfile dist/oxn-macos",
    "build:windows": "bun build ./src/cli/index.ts --compile --target=bun-windows-x64 --outfile dist/oxn.exe",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "biome format --write src/",
    "format:check": "biome format src/",
    "check": "biome check src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist node_modules",
    "prepare": "lefthook install",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "citty": "^0.1.6",
    "glob": "^13.0.6",
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@typescript-eslint/eslint-plugin": "^8.59.4",
    "@typescript-eslint/parser": "^8.59.4",
    "bun-types": "^1.1.38",
    "eslint": "^10.4.0",
    "langium": "^4.2.4",
    "langium-cli": "^4.2.1",
    "lefthook": "^2.1.8",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "cli",
    "ai",
    "llm",
    "engineering",
    "automation",
    "verification"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/openxenon/openxenon.git"
  },
  "bugs": {
    "url": "https://github.com/openxenon/openxenon/issues"
  },
  "homepage": "https://openxenon.dev"
}
