// Task: verify
// 阶段 4：回归验证

task "verify" blueprint "fix-issue" {
  inject "work-context";

  context {
    objective = "跑 vitest + 1 轮 e2e，确认 state 同步 bug 修复且未引入新 bug";
    constraints = [
      "所有测试必须 pass",
      "frozen.json 路径不能改"
    ];
  }

  slot "verify" {
    deps = ["fix"];
  }
}
