// Task: diagnose
// 阶段 1：复现 bug，记录现场

task "diagnose" blueprint "fix-issue" {
  inject "work-context";

  context {
    objective = "在 src/work/state-io.ts 与 dual-state-io.ts 上跑 1 轮 leader run + submit，确认 state.json 是否包含最新 completedParts";
    constraints = [
      "只读不写",
      "记录当前 dev/prod 环境的 state 形态差异"
    ];
  }

  slot "diagnose" {
    deps = [];
  }
}
