id: fs-exists
name: File System Exists Probe
type: fs_exists
description: 检查文件或目录是否存在
parameters:
  - name: pattern
    type: string
    description: 要检查的路径（相对于 projectRoot）
spec:
  description: 验证文件或目录存在性
  constraints:
    - "pattern 必须是字符串"
    - "pattern 不应为空"